# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Authentication
authlib>=1.3.0
httpx>=0.26.0
PyJWT>=2.8.0
itsdangerous>=2.1.2

# Scheduling
APScheduler>=3.10.4

# Templates & static files
jinja2>=3.1.3
aiofiles>=23.2.1

# Configuration
pyyaml>=6.0.1
pydantic>=2.5.0
pydantic-settings>=2.1.0

# File watching (for config hot-reload)
watchfiles>=0.21.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Existing workflow dependencies (used by imported Tools modules)
feedparser
lxml
spacy
# spaCy's CLI imports click directly (cli/_util.py), but its only path to
# click is via typer, and typer >=0.26 dropped click from its install_requires.
# Pinning typer would age badly; declaring the real dep explicitly is robust.
click
spacyfishing
numpy
aeneas
# PyMuPDF (imported as `fitz`) is the PDF extraction backend used by
# optv.shared.pdf2tei — required by DE-BY/BW/HH/NW/SH/SN/ST/RP parse stages.
pymupdf
jsonschema>=4.0
